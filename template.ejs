<!DOCTYPE html>
<html lang="ja">

<head>
	<meta charset="UTF-8" />
	<title>Document</title>
</head>

<body>
	<h1>掲示板</h1>
	<ul>
		<% for (const book of books) { %>
		<li>
			<form action="/edit">
				<b>
					<% if(book.name === ""){ %>
						名無し
					<% }else{ %>
						<%= book.name %>
					<% } %>
				:</b>
				<%= book.age %>
				<i style="color:gray">(
					<% if(book.edited){ %>
						編集済み:
					<% } %>
					<%= format(new Date(book.time), "y/M/d(eee) H:mm:ss") %>
				)</i>
				<input type="hidden" name="id" value="<%= book.id %>">
				<button>編集</button>
			</form>
		</li>
		<% } %>
	</ul>
	<form method="post" action="/send">
		<input placeholder="名前" name="name" />
		<input placeholder="コメント" name="age" />
		<button>送信</button>
	</form>
</body>

</html>
